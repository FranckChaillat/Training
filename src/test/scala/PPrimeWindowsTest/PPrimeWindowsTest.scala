package PPrimeWindowsTest

import org.scalatest.FlatSpec

class PPrimeWindowsTest extends FlatSpec{
  //  it should "return 4 primes numbers for range 3 - 11" in {
  //    val res = PprimesWindows.PPrimeWindows.countPrimeInRange(Array(1,2,3,5,7,11,13,17), 3, 11)
  //    assert(res == Some(4))
  //  }
  //
  //  it should "return 2 primes numbers for range 4 - 8" in {
  //    val res = PprimesWindows.PPrimeWindows.countPrimeInRange(Array(1,2,3,5,7,11,13,17), 4, 8)
  //    assert(res == Some(2))
  //  }
  //
  //  it should "return x primes numbers for range 2 - 8" in {
  //    val res = PprimesWindows.PPrimeWindows.countPrimeInRange(Array(2,3,5,7,11,13,17), 2, 5)
  //    assert(res == Some(3))
  //  }
  //
  //  it should "return x primes numbers for range 3 - 6" in {
  //    val res = PprimesWindows.PPrimeWindows.countPrimeInRange(Array(2,3,5,7,11,13,17), 3, 6)
  //    assert(res == Some(2))
  //  }
  //
  //  it should "return x primes numbers for range 4 - 7" in {
  //    val res = PprimesWindows.PPrimeWindows.countPrimeInRange(Array(2,3,5,7,11,13,17), 4, 7)
  //    assert(res == Some(2))
  //  }
  //
  //  it should "get prime numbers " in {
  //    val res = PprimesWindows.PPrimeWindows.getPrimes(Range(2, 8))
  //    assert(res.diff(Array(2,3,5,7)).isEmpty)
  //  }
}
